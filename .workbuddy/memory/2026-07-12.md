# 2026-07-12

## CDS original files -> /Users/ggdayup/Downloads/CDS_RAG
- Copied CDS **original source docs** from `data/cds/CollegeFlow_CDS/cds_data` (71 university subdirs) into `/Users/ggdayup/Downloads/CDS_RAG`.
- Selection rule: file basename matches `CDS_YYYY-YYYY.*<doc ext>` AND excludes derivative keywords (llamaparse, canonical, structured, raw_pages, ocr, corrected, preview, extracted, gemini, qwen, key_mapping, sample_layout, sys_raw_layout, verification, skill). Pipeline subdirs (gem/, llamaparse_tables/, pages_scratch/) not descended.
- Destination naming: `[{university_dir_basename}]_{original_filename}` (kept numeric index to disambiguate e.g. `32_uci` vs `36_uci`, which are the same UCI PDF, byte-identical).
- Result: 74 files copied (62 pdf, 8 xlsx, 2 html, 2 json), 0 skipped, 0 errors. 2 folders (`42_northeastern`, `46_fsu`) had no CDS original.
- Reports written: `CDS_RAG_MANIFEST.csv` (copy log) and `CDS_RAG_SKIPPED.txt` (conflict log, empty). Script: /tmp/cds_copy.py.

## CDS JSON/HTML -> Markdown (RAG ingest fix)
- Converted the 4 non-PDF originals in `/Users/ggdayup/Downloads/CDS_RAG` to `.md` so they can be ingested by RAG.
- Converter `/tmp/convert_cds.py` (managed venv + `html2text` at `/Users/ggdayup/.workbuddy/binaries/python/envs/default`):
  - Stanford-style JSON (top-level single-letter section dicts) -> nested bullet Markdown.
  - MIT-style JSON (`content_text` + `tables`) -> sectioned text + Markdown tables.
  - HTML -> `html2text`; mojibake cleanup via latin1->utf8 roundtrip + targeted replacements.
- Outputs (originals kept): `[02_mit]_CDS_2024-2025.md`, `[04_stanford]_CDS_2024-2025.md`, `[24_cmu]_CDS_2025-2026.md`, `[31_amherst]_CDS_2024-2025_Landing.md`.
- Note: CMU & Amherst "original" HTMLs are index/landing pages (links to per-section PDFs); their real data is in the already-present PDFs.

## CDS XLSX -> Markdown
- Converted 8 XLSX originals in `/Users/ggdayup/Downloads/CDS_RAG` to `.md` (Duke, Berkeley, Vanderbilt, UIUC, UMD, VT, WM, UC Merced).
- `/tmp/convert_xlsx.py` (openpyxl): each sheet -> `## Sheet` section; contiguous non-empty row blocks -> Markdown tables (single-row blocks -> bold headings). Mojibake cleanup reused. Title parsed from `[NN_name]` prefix.
- Folder now holds 12 .md files total (4 from json/html + 8 from xlsx) plus 74 originals + 2 reports.
